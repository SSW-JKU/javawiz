// https://github.com/node-gradle/gradle-node-plugin
plugins {
    id 'base'
    id "com.github.node-gradle.node" version "7.1.0"
}


node {
    version = '22.13.1'
}


build {
    doLast {
      println "Nothing to build, but existing build task ensures that npm install will be executed"
    }
}