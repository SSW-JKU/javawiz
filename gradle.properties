kotlin.code.style=official
# Reuse the configured task graph for all wrapper invocations (CLI, IDE, CI,
# development scripts, and release scripts). Use --no-configuration-cache for
# temporary troubleshooting of a task or third-party plugin.
org.gradle.configuration-cache=true
